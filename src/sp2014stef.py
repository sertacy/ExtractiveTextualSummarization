from multiprocessing import Pool
#from multiprocessing import Process
from evaluate import *

#if __name__ == '__main__':
 #   p = Pool(10)
    #p = Process(target=single_features, args= ('a'))
  #  p.map(single_features,[7])
    #p.map(find_best_combination,[2])
    
find_best_combination(7)